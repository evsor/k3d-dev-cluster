global:
  domain: argocd.local

configs:
  params:
    server.insecure: true

server:
  httproute:
    enabled: true
    parentRefs:
      - name: shared-gateway
        namespace: default
        sectionName: http
    hostnames:
      - argocd.local