global:
  domain: argocd.local

configs:
  params:
    server.insecure: true

server:
  httproute:
    enabled: true
    parentRefs:
      - name: demo-gateway
        namespace: demo
        sectionName: http
    hostnames:
      - argocd.local