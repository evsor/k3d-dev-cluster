apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: zot-container-registry
spec:
  parentRefs:
  - name: shared-gateway
    namespace: default
    sectionName: https
  hostnames:
  - zot.local
  rules:
  - backendRefs:
    - name: zot-container-registry
      port: 5000