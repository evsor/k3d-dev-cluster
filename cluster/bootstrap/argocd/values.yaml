fullnameOverride: "argocd"

global:
  domain: argocd.local

configs:
  params:
    server.insecure: false

server:
  httproute:
    enabled: true
    parentRefs:
      - name: shared-gateway
        namespace: default
        sectionName: https
    hostnames:
      - argocd.local